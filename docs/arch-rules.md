# arch-rules.md — Проектные правила

## 📚 Оглавление

- [1. Стек и версии](#1-стек-и-версии)
- [2. Поддерживаемые сети](#2-поддерживаемые-сети)
- [3. Безопасность и приватность](#3-безопасность-и-приватность)
- [4. Архитектура и кодстайл](#4-архитектура-и-кодстайл)
- [5. React/UI](#5-reactui)
- [6. Блокчейн-интеграция](#6-блокчейн-интеграция)
- [7. Данные и цены](#7-данные-и-цены)
- [8. Git-процесс](#8-git-процесс)

---

## 1. Стек и версии

### ✅ Обязательные технологии

| Компонент | Требование | Запрещено |
|-----------|------------|-----------|
| **Язык программирования** | TypeScript (строго) | JavaScript |
| **Фреймворк** | Next.js 14+ (App Router) | Pages Router |
| **Web3** | `wagmi v2` + `viem` | `ethers.js`, прямой `window.ethereum`* |
| **Менеджер пакетов** | `npm` | `pnpm`, `yarn` |
| **Node.js** | `>= 20.x` | Более старые версии |
| **CSS** | TailwindCSS + PostCSS | CSS-in-JS без согласования |
| **Деплой** | Vercel / Netlify | Не согласованные платформы |

> **Примечание:** *Прямой доступ к `window.ethereum` разрешён только в узких случаях и только после code review.

### 📝 Конфигурация

- Зафиксировать версию Node.js через `.nvmrc`
- Целевая платформа деплоя должна быть описана в `README.md`

---

## 2. Поддерживаемые сети

### 🌐 Blockchain Networks

| Среда | Сеть | Chain ID |
|-------|------|----------|
| **Тестирование** | Sepolia | `11155111` |
| **Продакшен** | Ethereum Mainnet | `1` |

### 📋 Правила работы с сетями

1. **Централизация конфигурации**
   - Все chain-константы хранятся в `src/config/chains.ts`

2. **Переключение сетей**
   - Автоматическое переключение через `wagmi`/`switchChain`
   - Чёткие и понятные сообщения пользователю

3. **Неподдерживаемые сети**
   - UI переходит в режим `read-only`
   - Отображается подсказка о необходимости переключения

---

## 3. Безопасность и приватность

### 🔒 Критические правила безопасности

#### ❌ Запрещено

- Запрашивать seed-фразы или приватные ключи
- Коммитить `.env*` файлы (обязательно в `.gitignore`)
- Логировать адреса кошельков в явном виде
- Подключать сторонние скрипты без code review

#### ✅ Обязательно

1. **Аутентификация**
   - Только через `Connect Wallet`
   - Без хранения приватных данных

2. **Секреты и ключи**
   - Хранение только в `.env` / переменных окружения
   - RPC-ключи (Infura/Alchemy) с domain restriction и квотами

3. **HTTP Headers**
   ```
   Content-Security-Policy: ...
   X-Frame-Options: DENY
   Referrer-Policy: strict-origin-when-cross-origin
   ```

4. **Cookies**
   - `SameSite=Lax`
   - Только через HTTPS

5. **Логирование**
   - Masking персональных данных
   - Без явных адресов кошельков

6. **Веб-аналитика**
   - Только опционально
   - Без идентификаторов кошельков

7. **Валидация данных**
   - Sanitize всех входных данных (Form/URL)
   - Ограничения длины полей

---

## 4. Архитектура и кодстайл

### 📁 Структура проекта

```
project/
├── app/              # Страницы (Next.js App Router)
├── components/       # React-компоненты
├── lib/              # Утилиты, wagmi, API
├── config/           # Конфигурация (chains, token, etc.)
├── hooks/            # Custom React hooks
└── styles/           # Глобальные стили
```

### 🔧 Конфигурация импортов

- **Абсолютные импорты** через `@/lib/...`
- Настройка в `tsconfig.json`

```typescript
// ✅ Правильно
import { wagmiConfig } from '@/lib/wagmi'

// ❌ Неправильно
import { wagmiConfig } from '../../../lib/wagmi'
```

### 📏 Code Style

#### Инструменты

- **ESLint** — линтинг
- **Prettier** — форматирование
- **EditorConfig** — единая конфигурация редакторов

#### Правила TypeScript

| Правило | Требование |
|---------|------------|
| **Типизация props** | Обязательна |
| **Типизация return** | Обязательна для публичных функций |
| **`any`** | Запрещён |
| **`strictNullChecks`** | `true` |
| **`noImplicitReturns`** | `true` |
| **`import/order`** | Обязательна сортировка |
| **Циклические зависимости** | Запрещены |

#### Пример правильной типизации

```typescript
// ✅ Правильно
interface ButtonProps {
  label: string;
  onClick: () => void;
}

function Button({ label, onClick }: ButtonProps): JSX.Element {
  return <button onClick={onClick}>{label}</button>;
}

// ❌ Неправильно
function Button({ label, onClick }: any) {
  return <button onClick={onClick}>{label}</button>;
}
```

### 🌍 Интернационализация

- Централизованное хранение переводов
- Рекомендуется: `next-intl` или простой словарь

---

## 5. React/UI

### ⚛️ Принципы разработки компонентов

#### Чистота компонентов

- **Компоненты** — только для UI
- **Бизнес-логика** — в custom hooks
- **Side-effects** — в отдельных hooks

#### Server vs Client Components

```typescript
// ✅ Правильно: серверный компонент по умолчанию
export default function Layout({ children }: { children: React.ReactNode }) {
  return <div>{children}</div>;
}

// ✅ Правильно: клиентский компонент только когда нужно
'use client'
export default function WalletButton() {
  const { connect } = useConnect();
  return <button onClick={() => connect()}>Connect</button>;
}
```

#### Управление состоянием Web3

- **Только `wagmi`** + **React-Query**
- ❌ Запрещены ручные `useEffect` для RPC-запросов

### 📱 Адаптивность

- **Mobile First** подход
- Десктоп: `>= 1280px`
- Тестирование на всех breakpoints

### ♿ Доступность (A11y)

| Требование | Стандарт |
|------------|----------|
| **Контрастность** | WCAG 2.1 AA (≥ 4.5:1) |
| **Фокус-стили** | Обязательны |
| **Клавиатурная навигация** | Полная поддержка |
| **ARIA-метки** | Для интерактивных элементов |

---

## 6. Блокчейн-интеграция

### 🔗 Работа с блокчейном

#### Используемые библиотеки

**Только `wagmi` + `viem`**

| Операция | Hook | Применение |
|----------|------|------------|
| **Чтение данных** | `useReadContract` | Баланс, комиссии, параметры |
| **Запись данных** | `useWriteContract` | Транзакции, approve |
| **Кеширование** | React-Query | Автоматически через wagmi |

#### Конфигурация токена

Все параметры в `src/config/token.ts`:

```typescript
export const TOKEN_CONFIG = {
  address: process.env.NEXT_PUBLIC_TOKEN_ADDRESS as `0x${string}`,
  decimals: 18,
  symbol: 'TKN',
  abi: ERC20_ABI,
} as const;
```

### ⚠️ Обработка отсутствия контракта

Если `TOKEN_ADDRESS` не задан:

- UI показывает статус **"Ожидаем деплой"**
- Все кнопки взаимодействия недоступны
- Отображается информационное сообщение

### 💰 Работа с суммами

```typescript
// ✅ Правильно
import { formatUnits, parseUnits } from 'viem';

const amount = parseUnits('1.5', 18); // BigInt
const formatted = formatUnits(amount, 18); // string

// ❌ Неправильно
const amount = 1.5 * 10**18; // Потеря точности!
```

### 📊 Отображение комиссий (Tax)

- Источник: контракт или конфиг
- Отображение в UI текстом (например, "2% комиссия")

### 🔔 Подтверждение транзакций

**Обязательно:**

- Модальное окно подтверждения
- Отображение суммы и комиссии
- Toast-уведомления о статусе
- ❌ Запрещены транзакции без явного UX-подтверждения

---

## 7. Данные и цены

### 💵 Пересчёт в USD

#### Режим `fixed`

```env
NEXT_PUBLIC_TOKEN_PRICE_USD=1.50
```

#### Режим `coingecko` (или custom API)

- Реализация в `lib/pricing.ts`
- Кеширование: **60–120 секунд**
- Обработка ошибок с fallback

```typescript
// Пример структуры
export async function getTokenPrice(): Promise<number> {
  try {
    // API запрос с таймаутом
    const price = await fetchPrice({ timeout: 5000 });
    return price;
  } catch (error) {
    console.error('Price fetch failed:', error);
    return parseFloat(process.env.NEXT_PUBLIC_TOKEN_PRICE_USD || '0');
  }
}
```

### 🌐 Внешние API

**Обязательные требования:**

- Таймауты на запросы
- Try-catch обёртки
- Graceful fallback при ошибках
- Логирование проблем

---

## 8. Git-процесс

### 🌿 Стратегия ветвления

| Ветка | Назначение | Защита |
|-------|------------|--------|
| `main` | Продакшен | ✅ Защищена |
| `develop` | Разработка | По желанию |
| `feature/*` | Новые фичи | - |
| `fix/*` | Исправления | - |
| `hotfix/*` | Срочные исправления | - |

### 📝 Conventional Commits

Формат: `<type>: <description>`

| Тип | Применение | Пример |
|-----|------------|--------|
| `feat` | Новая функциональность | `feat: add wallet connect button` |
| `fix` | Исправление бага | `fix: correct balance calculation` |
| `docs` | Документация | `docs: update README with setup` |
| `refactor` | Рефакторинг | `refactor: extract pricing logic` |
| `chore` | Рутинные задачи | `chore: update dependencies` |
| `test` | Тесты | `test: add balance display tests` |
| `build` | Сборка/CI | `build: configure vercel deployment` |

### 🔍 Pull Request чеклист

Создайте файл `.github/PULL_REQUEST_TEMPLATE.md`:

```markdown
## Описание изменений
<!-- Кратко опишите, что изменено и зачем -->

## Тип изменений
- [ ] 🐛 Bug fix
- [ ] ✨ New feature
- [ ] 🔨 Refactoring
- [ ] 📝 Documentation
- [ ] 🎨 UI/Styling

## Чеклист
- [ ] Код следует project style guide
- [ ] Добавлены/обновлены типы TypeScript
- [ ] Проведено self-review кода
- [ ] Код прокомментирован в сложных местах
- [ ] Обновлена документация (если нужно)
- [ ] Проверена работа на мобильных устройствах
- [ ] Проверена доступность (a11y)
- [ ] Нет warnings в консоли
- [ ] Проверена работа с Sepolia testnet

## Скриншоты (если применимо)
<!-- Добавьте скриншоты для UI-изменений -->

## Связанные issues
<!-- Ссылки на связанные задачи -->
Closes #
```

### 🔐 Защита веток

**Настройки для `main`:**

- Требуется code review (минимум 1 approval)
- Все проверки CI должны пройти
- Нельзя force push
- Нельзя удалить ветку

---

## 📌 Дополнительные материалы

### Рекомендуемые расширения VSCode

```json
{
  "recommendations": [
    "dbaeumer.vscode-eslint",
    "esbenp.prettier-vscode",
    "bradlc.vscode-tailwindcss",
    "ms-vscode.vscode-typescript-next"
  ]
}
```

### Полезные ссылки

- [wagmi Documentation](https://wagmi.sh/)
- [viem Documentation](https://viem.sh/)
- [Next.js Documentation](https://nextjs.org/docs)
- [TailwindCSS Documentation](https://tailwindcss.com/docs)
- [WCAG 2.1 Guidelines](https://www.w3.org/WAI/WCAG21/quickref/)

---

**Версия документа:** 1.0  
**Последнее обновление:** 23 октября 2025  
**Статус:** Действующий

> ⚠️ **Важно:** Все разработчики обязаны следовать этим правилам. Отклонения возможны только после обсуждения и одобрения tech lead.
