# Архитектура веб-кошелька

## 📋 Обзор архитектуры

**Проект:** Внутренний веб-интерфейс для управления токенами компании  
**Архитектурный подход:** Модульная архитектура с четким разделением ответственности  
**Паттерн:** Component-Based Architecture с Custom Hooks

---

## 🏗️ Структура проекта

```
web-wallet/
├── app/                    # Next.js App Router страницы
│   ├── layout.tsx          # Корневой layout с провайдерами
│   ├── page.tsx            # Главная страница дашборда
│   └── globals.css         # Глобальные стили
├── components/             # React компоненты
│   ├── ui/                # Базовые UI компоненты
│   │   ├── Button.tsx     # Кнопка с вариантами стилей
│   │   ├── Card.tsx       # Карточка для отображения информации
│   │   ├── Modal.tsx      # Модальное окно
│   │   └── Toast.tsx      # Уведомления
│   ├── wallet/            # Компоненты для работы с кошельком
│   │   ├── ConnectButton.tsx    # Кнопка подключения MetaMask
│   │   ├── WalletInfo.tsx       # Информация о кошельке
│   │   ├── BalanceCard.tsx      # Отображение баланса токена
│   │   ├── NetworkSwitcher.tsx  # Переключение сетей
│   │   └── TransactionModal.tsx # Модальное окно транзакций
│   └── chatbot/           # Компоненты чат-бота (anti-fraud)
│       ├── chat-widget.tsx      # Float кнопка чата
│       ├── chat-window.tsx      # Окно чата
│       ├── chat-message.tsx     # Компонент сообщения
│       ├── chat-input.tsx       # Поле ввода
│       └── translate-button.tsx  # Кнопка перевода
├── lib/                   # Утилиты и конфигурация
│   ├── wagmi.tsx          # Конфигурация wagmi клиента
│   ├── pricing.ts         # Логика ценообразования
│   └── abi/               # ABI контрактов
│       └── erc20.ts       # ABI ERC-20 токена
├── config/                # Конфигурационные файлы
│   ├── chains.ts          # Настройки блокчейн сетей
│   └── token.ts           # Конфигурация токена
├── hooks/                 # Custom React hooks
│   ├── useTokenBalance.ts # Hook для получения баланса
│   ├── useTokenInfo.ts    # Hook для информации о токене
│   ├── useWalletConnection.ts # Hook для подключения кошелька
│   ├── useTokenPrice.ts   # Hook для получения цены токена
│   ├── use-chatbot.ts     # Логика чат-бота
│   ├── use-transaction-analysis.ts # Анализ транзакций
│   └── use-chat-history.ts # История чата
├── styles/                # Глобальные стили
│   └── globals.css        # Основные стили и переменные
└── docs/                  # Документация
    ├── requirements.md    # Технические требования
    ├── arch-rules.md     # Архитектурные правила
    ├── architecture.md   # Этот файл
    ├── plan.md           # План реализации
    └── chatbot-implementation-plan.md # План чат-бота
```

---

## 🔧 Техническая архитектура

### Стек технологий

| Компонент              | Технология   | Версия | Назначение                         |
| ---------------------- | ------------ | ------ | ---------------------------------- |
| **Frontend Framework** | Next.js      | 14+    | Основной фреймворк с App Router    |
| **Language**           | TypeScript   | 5+     | Типизированный JavaScript          |
| **Web3 Integration**   | wagmi + viem | 2.x    | Взаимодействие с блокчейном        |
| **State Management**   | React Query  | 5.x    | Кеширование и синхронизация данных |
| **Styling**            | TailwindCSS  | 3.x    | Utility-first CSS фреймворк        |
| **Package Manager**    | pnpm         | Latest | Менеджер пакетов                   |
| **Runtime**            | Node.js      | 20+    | Среда выполнения                   |

### Архитектурные принципы

#### 1. **Разделение ответственности**

- **Компоненты** — только UI логика и отображение
- **Hooks** — бизнес-логика и состояние
- **Utils** — вспомогательные функции
- **Config** — конфигурация приложения

#### 2. **Server vs Client Components**

```typescript
// Server Component (по умолчанию)
export default function Layout({ children }: { children: React.ReactNode }) {
  return <div>{children}</div>;
}

// Client Component (только когда нужно)
'use client'
export default function WalletButton() {
  const { connect } = useConnect();
  return <button onClick={() => connect()}>Connect</button>;
}
```

#### 3. **Типизация**

- Строгая типизация всех компонентов и функций
- Запрет на использование `any`
- Обязательная типизация props и return значений

---

## 🔗 Интеграция с блокчейном

### Архитектура Web3 интеграции

```
┌─────────────────┐    ┌─────────────────┐    ┌─────────────────┐
│   React UI      │    │   wagmi Hooks   │    │   viem Client   │
│                 │◄──►│                 │◄──►│                 │
│ - Components    │    │ - useConnect    │    │ - RPC Calls     │
│ - Hooks         │    │ - useReadContract│    │ - Contract ABI  │
│ - State         │    │ - useWriteContract│   │ - Chain Config  │
└─────────────────┘    └─────────────────┘    └─────────────────┘
         │                       │                       │
         ▼                       ▼                       ▼
┌─────────────────┐    ┌─────────────────┐    ┌─────────────────┐
│   Local State   │    │   React Query   │    │   Blockchain    │
│                 │    │                 │    │                 │
│ - UI State      │    │ - Caching       │    │ - Sepolia/Eth   │
│ - Form State    │    │ - Synchronization│    │ - Smart Contracts│
└─────────────────┘    └─────────────────┘    └─────────────────┘
```

### Поддерживаемые сети

| Сеть         | Chain ID | Назначение   | RPC URL                       |
| ------------ | -------- | ------------ | ----------------------------- |
| **Sepolia**  | 11155111 | Тестирование | `NEXT_PUBLIC_RPC_URL_SEPOLIA` |
| **Ethereum** | 1        | Продакшен    | `NEXT_PUBLIC_RPC_URL_MAINNET` |

### Конфигурация wagmi

```typescript
// lib/wagmi.tsx
import { createConfig, http } from "wagmi";
import { sepolia, mainnet } from "wagmi/chains";

export const config = createConfig({
  chains: [sepolia, mainnet],
  transports: {
    [sepolia.id]: http(process.env.NEXT_PUBLIC_RPC_URL_SEPOLIA),
    [mainnet.id]: http(process.env.NEXT_PUBLIC_RPC_URL_MAINNET),
  },
});
```

---

## 🎨 UI/UX Архитектура

### Дизайн-система

#### Цветовая палитра

```css
:root {
  --bg-primary: #0a0a0a; /* Основной фон */
  --bg-secondary: #1f1f1f; /* Карточки и элементы */
  --accent-primary: #10b981; /* Акцентный цвет */
  --text-primary: #ffffff; /* Основной текст */
  --text-secondary: #a1a1aa; /* Вторичный текст */
  --border: #27272a; /* Границы */
}
```

#### Компонентная система

- **Button** — базовый компонент кнопки с вариантами
- **Card** — контейнер для группировки контента
- **Modal** — модальные окна для подтверждений
- **Toast** — уведомления о статусе операций

### Адаптивность

- **Mobile First** подход
- Breakpoints: `sm: 640px`, `md: 768px`, `lg: 1024px`, `xl: 1280px`
- Поддержка всех устройств от 320px до 1920px+

---

## 🔒 Безопасность

### Архитектурные меры безопасности

#### 1. **Аутентификация**

- Только через MetaMask Connect
- Никакого хранения приватных ключей
- Валидация всех подключений

#### 2. **Сетевая безопасность**

- Работа только в поддерживаемых сетях
- Автоматическое переключение сетей
- Проверка Chain ID перед операциями

#### 3. **Валидация данных**

- Типизация всех входных данных
- Санитизация пользовательского ввода
- Ограничения на длину полей

#### 4. **HTTP Headers**

```typescript
// next.config.ts
const securityHeaders = [
  {
    key: "Content-Security-Policy",
    value: "default-src 'self'; script-src 'self' 'unsafe-eval' 'unsafe-inline'",
  },
  {
    key: "X-Frame-Options",
    value: "DENY",
  },
  {
    key: "Referrer-Policy",
    value: "strict-origin-when-cross-origin",
  },
];
```

---

## 📊 Управление состоянием

### Архитектура состояния

```
┌─────────────────────────────────────────────────────────────┐
│                    React Query Cache                        │
├─────────────────────────────────────────────────────────────┤
│ • Token Balance    • Network Info    • Transaction Status   │
│ • Token Price      • Wallet Address  • Error States         │
│ • Contract Data    • User Preferences • Loading States      │
└─────────────────────────────────────────────────────────────┘
                              │
                              ▼
┌─────────────────────────────────────────────────────────────┐
│                  Local Component State                      │
├─────────────────────────────────────────────────────────────┤
│ • Form Data       • UI Toggles      • Modal States          │
│ • Input Values    • Loading States  • Error Messages        │
└─────────────────────────────────────────────────────────────┘
```

### Custom Hooks

#### useTokenBalance

```typescript
export function useTokenBalance(address: `0x${string}`) {
  return useReadContract({
    address: TOKEN_CONFIG.address,
    abi: TOKEN_CONFIG.abi,
    functionName: "balanceOf",
    args: [address],
    query: {
      refetchInterval: 10000, // 10 секунд
    },
  });
}
```

#### useWalletConnection

```typescript
export function useWalletConnection() {
  const { connect, connectors } = useConnect();
  const { disconnect } = useDisconnect();
  const { address, isConnected } = useAccount();

  return {
    connect,
    disconnect,
    address,
    isConnected,
    connectors,
  };
}
```

---

## 🔄 Жизненный цикл приложения

### Инициализация

1. **Загрузка конфигурации** — сети, токены, RPC URLs
2. **Инициализация wagmi** — подключение к блокчейну
3. **Настройка провайдеров** — React Query, Theme
4. **Загрузка UI** — компоненты и стили

### Рабочий процесс

1. **Пользователь заходит** → Проверка подключения кошелька
2. **Подключение MetaMask** → Получение адреса и сети
3. **Проверка сети** → Переключение на поддерживаемую
4. **Загрузка данных** → Баланс, цена, информация о токене
5. **Отображение UI** → Дашборд с актуальными данными

### Обработка ошибок

- **Network Errors** → Автоматическое переключение сети
- **Contract Errors** → Отображение понятных сообщений
- **Connection Errors** → Предложение переподключения

---

## 📈 Производительность

### Оптимизация

#### 1. **Кеширование**

- React Query для кеширования блокчейн данных
- Статические ресурсы через Next.js
- Локальное хранение настроек пользователя

#### 2. **Lazy Loading**

- Динамическая загрузка компонентов
- Разделение кода по маршрутам
- Оптимизация изображений

#### 3. **Bundle Optimization**

- Tree shaking неиспользуемого кода
- Минификация CSS и JS
- Сжатие статических ресурсов

---

## 🚀 Деплой и масштабирование

### Готовность к продакшену

#### Переменные окружения

```env
# Blockchain
NEXT_PUBLIC_TOKEN_ADDRESS=0x...
NEXT_PUBLIC_TOKEN_CHAIN_ID=1
NEXT_PUBLIC_TOKEN_PRICE_USD=1.50
NEXT_PUBLIC_RPC_URL_SEPOLIA=https://sepolia.infura.io/v3/KEY
NEXT_PUBLIC_RPC_URL_MAINNET=https://mainnet.infura.io/v3/KEY

# App
NEXT_PUBLIC_APP_NAME="Company Token Wallet"
NEXT_PUBLIC_APP_DESCRIPTION="Internal token management system"
```

#### Платформы деплоя

- **Основная:** Vercel (рекомендуется)
- **Альтернатива:** Netlify
- **Собственный сервер:** Docker + Nginx

### Мониторинг

- Логирование ошибок
- Аналитика производительности
- Мониторинг доступности

---

## 🔄 Эволюция архитектуры

### Планы развития

1. **Фаза 1** — Базовая функциональность (текущая)
2. **Фаза 2** — Расширенные операции с токеном
3. **Фаза 3** — Поддержка множественных токенов
4. **Фаза 4** — Интеграция с внешними API

### Принципы развития

- Обратная совместимость
- Модульность архитектуры
- Постепенная миграция
- Тестирование изменений

---

**Версия документа:** 1.0  
**Последнее обновление:** 23 октября 2025  
**Статус:** Актуальный

> ⚠️ **Важно:** Архитектура должна следовать принципам, описанным в `arch-rules.md`, и требованиям из `requirements.md`.
