# ✅ Чеклист готовности внутреннего баланса к тестированию

## 🔴 КРИТИЧЕСКИЕ ПРОБЛЕМЫ (нужно исправить перед тестированием)

### ⚠️ Проблема 1: TREASURY_PRIVATE_KEY

**Текущее значение:** `0x60baea8e612f41bf6034056fe5f3eacbc8ee6716`

**Проблема:** Это выглядит как **адрес кошелька** (42 символа), а не как **приватный ключ** (64 символа hex).

**Правильный формат приватного ключа:**

- Должен начинаться с `0x`
- Должен содержать ровно **64 символа hex** после `0x`
- Пример: `0x1234567890abcdef1234567890abcdef1234567890abcdef1234567890abcdef`

**Как исправить:**

1. Откройте MetaMask
2. Выберите кошелек казначейства
3. Нажмите `⋯` → `Account details` → `Export private key`
4. Скопируйте приватный ключ (64 символа без пробелов)
5. Добавьте `0x` в начало
6. Обновите `.env.local`:
   ```env
   TREASURY_PRIVATE_KEY=0x<ваш_полный_приватный_ключ_64_символа>
   ```

### ⚠️ Проблема 2: TREASURY_RPC_URL

**Текущее значение:** `0f8e0a364e954c79949854942427705b`

**Проблема:** Это выглядит как часть ключа API, а не как полный RPC URL.

**Правильный формат RPC URL:**

- Должен быть полным URL с протоколом `https://`
- Примеры:
  - Infura: `https://mainnet.infura.io/v3/YOUR_INFURA_KEY`
  - Alchemy: `https://eth-mainnet.g.alchemy.com/v2/YOUR_ALCHEMY_KEY`
  - Public: `https://eth.llamarpc.com`

**Как исправить:**

1. Зарегистрируйтесь на Infura (https://infura.io) или Alchemy (https://www.alchemy.com)
2. Создайте проект/приложение
3. Скопируйте полный RPC URL
4. Обновите `.env.local`:
   ```env
   TREASURY_RPC_URL=https://mainnet.infura.io/v3/YOUR_FULL_KEY_HERE
   ```

---

## ✅ Проверка компонентов системы

### 1. База данных

- [x] Таблицы созданы (`internal_wallets`, `internal_balances`, `internal_ledger`, `withdraw_requests`)
- [x] Миграция применяется автоматически
- [ ] **Проверить:** Таблицы существуют в базе данных

**Проверка:**

```sql
SELECT table_name
FROM information_schema.tables
WHERE table_name IN ('internal_wallets', 'internal_balances', 'internal_ledger', 'withdraw_requests');
```

### 2. API эндпоинты

- [x] `GET /api/internal-balance` - получение баланса
- [x] `POST /api/internal-balance/credit` - начисление баланса
- [x] `POST /api/internal-balance/debit` - списание баланса
- [x] `POST /api/internal-balance/withdraw` - создание заявки на вывод
- [x] `GET /api/internal-balance/withdraw` - история заявок
- [x] `PATCH /api/internal-balance/withdraw/:id` - изменение статуса
- [x] `GET /api/internal-balance/withdraw/report` - CSV отчет

### 3. UI компоненты

- [x] `WalletStatistics` - отображение баланса
- [x] `InternalPayoutForm` - форма вывода
- [x] `useInternalBalance` - хук для баланса
- [x] `useWithdrawRequests` - хук для заявок

### 4. Автоматизация

- [x] Скрипт `scripts/process-withdrawals.ts`
- [x] Команда `npm run treasury:process`
- [x] Уведомления в Telegram

### 5. Переменные окружения

#### Обязательные:

- [x] `INTERNAL_BALANCE_SIGNING_SECRET` ✅ (заполнен)
- [ ] `TREASURY_PRIVATE_KEY` ⚠️ (исправить формат)
- [ ] `TREASURY_RPC_URL` ⚠️ (исправить формат)

#### Рекомендуемые:

- [x] `TREASURY_WORKER_ID` ✅ (заполнен: `treasury-bot`)
- [x] `TREASURY_BATCH_SIZE` ✅ (заполнен: `5`)
- [x] `TREASURY_TX_CONFIRMATIONS` ✅ (заполнен: `1`)
- [ ] `INTERNAL_WITHDRAW_DAILY_LIMIT` (опционально)
- [ ] `INTERNAL_WITHDRAW_MONTHLY_LIMIT` (опционально)
- [ ] `TREASURY_BLOCKED_ADDRESSES` (опционально)

#### Для Telegram бота (опционально):

- [ ] `TELEGRAM_API_KEY`
- [ ] `TELEGRAM_ADMIN_CHAT_ID`
- [ ] `TELEGRAM_ALLOWED_USER_ID`

---

## 🧪 План тестирования

### Этап 1: Базовая проверка (без реальных выплат)

#### 1.1 Проверка переменных окружения

```bash
# Проверить наличие обязательных переменных
grep -E "INTERNAL_BALANCE_SIGNING_SECRET" .env.local

# Должен вывести значение
```

#### 1.2 Проверка базы данных

```sql
-- Проверить существование таблиц
SELECT table_name
FROM information_schema.tables
WHERE table_name IN ('internal_wallets', 'internal_balances', 'internal_ledger', 'withdraw_requests');

-- Должно вернуть 4 строки
```

#### 1.3 Тест начисления баланса (через API)

```bash
curl -X POST http://localhost:3000/api/internal-balance/credit \
  -H "Content-Type: application/json" \
  -H "x-internal-admin-token: 1bff10f7b57539b14b09d88d8feb6e29f06c3ebfd6bb09b747a6c3567c4f160b" \
  -d '{
    "walletAddress": "0x742d35Cc6634C0532925a3b844Bc9e7595f0bEb",
    "amount": "100",
    "reference": "Test credit",
    "createdBy": "admin"
  }'
```

**Ожидаемый результат:**

- Статус 200 OK
- JSON с обновленным балансом

#### 1.4 Проверка отображения баланса на сайте

1. Откройте сайт
2. Подключите кошелек или войдите через email
3. Перейдите на страницу с `WalletStatistics`
4. Проверьте отображение внутреннего баланса

**Ожидаемый результат:**

- Виден баланс (100 токенов)
- Видна история операций

### Этап 2: Тестирование вывода (dry-run режим)

#### 2.1 Создание заявки на вывод

1. Откройте форму `InternalPayoutForm`
2. Введите сумму (например, 50 токенов)
3. Введите адрес кошелька для вывода
4. Отправьте заявку

**Ожидаемый результат:**

- Заявка создана со статусом `pending`
- Баланс уменьшился на сумму заявки
- Появилась запись в `pending_onchain`

#### 2.2 Одобрение заявки (через API)

```bash
curl -X PATCH http://localhost:3000/api/internal-balance/withdraw/<REQUEST_ID> \
  -H "Content-Type: application/json" \
  -H "x-internal-admin-token: 1bff10f7b57539b14b09d88d8feb6e29f06c3ebfd6bb09b747a6c3567c4f160b" \
  -d '{
    "status": "approved",
    "reviewerId": "admin"
  }'
```

**Ожидаемый результат:**

- Статус заявки изменен на `approved`
- Уведомление в Telegram (если настроено)

#### 2.3 Обработка заявки (dry-run)

```bash
npm run treasury:process
```

**Ожидаемый результат (dry-run режим):**

- Заявка обработана
- Статус изменен на `completed`
- Сгенерирован случайный txHash (не реальная транзакция)
- Баланс списан

### Этап 3: Тестирование с реальными выплатами (после исправления переменных)

⚠️ **ВНИМАНИЕ:** Этот этап только после исправления `TREASURY_PRIVATE_KEY` и `TREASURY_RPC_URL`!

#### 3.1 Проверка кошелька казначейства

- Убедитесь, что на кошельке есть токены для выплат
- Проверьте баланс кошелька

#### 3.2 Тест реальной выплаты

1. Создайте заявку на вывод
2. Одобрите заявку
3. Запустите `npm run treasury:process`
4. Проверьте транзакцию в блокчейн-эксплорере

**Ожидаемый результат:**

- Реальная транзакция отправлена в блокчейн
- Статус заявки `completed`
- Реальный txHash в базе данных
- Токены переведены на указанный адрес

---

## 📋 Итоговый чеклист перед тестированием

### Обязательно исправить:

- [ ] **TREASURY_PRIVATE_KEY** - должен быть полным приватным ключом (64 символа hex)
- [ ] **TREASURY_RPC_URL** - должен быть полным URL с `https://`

### Проверить:

- [ ] Таблицы базы данных созданы
- [ ] Сервер запущен и работает
- [ ] Переменные окружения загружены

### Готово к тестированию:

- [x] Код реализован
- [x] API эндпоинты работают
- [x] UI компоненты готовы
- [x] Скрипт обработки готов
- [x] Документация создана

---

## 🚨 Важные замечания

1. **Безопасность:**
   - Никогда не коммитьте приватные ключи в git
   - Используйте отдельный кошелек для выплат
   - Храните `.env.local` в безопасности

2. **Dry-run режим:**
   - Если `TREASURY_PRIVATE_KEY` или `TREASURY_RPC_URL` не указаны, система работает в dry-run
   - В dry-run режиме транзакции не отправляются в блокчейн
   - Это безопасно для тестирования

3. **Тестирование:**
   - Начните с dry-run режима
   - Протестируйте все функции
   - Только потом переходите к реальным выплатам

---

## 📞 Поддержка

Если возникнут проблемы:

1. Проверьте логи сервера
2. Проверьте логи скрипта `npm run treasury:process`
3. Проверьте базу данных
4. См. документацию: `docs/internal-balance-status-report.md`
