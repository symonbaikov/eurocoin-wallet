# Техническое задание: Система торговли токенами

## 📋 Обзор проекта

**Цель:** Реализация гибридной системы торговли токенами TOKEN ↔ EUR с автоматическим обменом и возможностью выставлять ордера через централизованный пул ликвидности компании.

**Тип решения:** Гибридный (автоматический обмен + ордербук)  
**Торговая пара:** TOKEN ↔ EUR  
**Управление ликвидностью:** Централизованный пул компании

**Дата создания:** 2025-01-XX  
**Версия:** 1.0

---

## 🎯 Цели и требования

### Основные цели

1. **Автоматический обмен** — пользователи могут мгновенно обменять токены на EUR по текущему курсу компании
2. **Ордербук** — пользователи могут выставлять ордера на покупку/продажу по желаемой цене
3. **Централизованный пул** — компания управляет ликвидностью и курсом обмена
4. **Прозрачность** — все операции видны в истории, статусы обновляются в реальном времени
5. **Безопасность** — защита от мошенничества, валидация всех операций

### Функциональные требования

#### FR-1: Автоматический обмен
- Пользователь указывает количество токенов или EUR для обмена
- Система автоматически рассчитывает курс с учетом комиссии
- Мгновенное исполнение при наличии ликвидности в пуле
- Автоматическое списание/начисление токенов через смарт-контракт
- Уведомления о статусе операции

#### FR-2: Ордербук
- Пользователь может выставить ордер на покупку (BUY) или продажу (SELL)
- Указание желаемой цены и количества
- Отображение активных ордеров других пользователей
- Автоматическое сопоставление ордеров (matching)
- Возможность отмены собственных ордеров

#### FR-3: Управление пулом ликвидности
- Администратор может управлять размером пула
- Настройка курса обмена (спред bid/ask)
- Мониторинг баланса пула
- Автоматические уведомления при низком балансе

#### FR-4: История операций
- История всех обменов пользователя
- История ордеров с деталями исполнения
- Фильтрация по типу операции, статусу, дате
- Экспорт истории в CSV/PDF

---

## 🏗️ Архитектура решения

### Общая схема

```
┌─────────────────┐     ┌──────────────────┐     ┌─────────────────┐
│   Frontend      │────▶│   Backend API    │────▶│  Database       │
│  (Trading UI)   │     │   (Next.js)       │     │  (PostgreSQL)   │
└─────────────────┘     └──────────────────┘     └─────────────────┘
         │                       │                          │
         │                       ▼                          │
         │              ┌──────────────────┐                │
         │              │  Smart Contract  │                │
         │              │   (ERC-20)       │                │
         │              └──────────────────┘                │
         │                       │                          │
         ▼                       ▼                          ▼
┌─────────────────┐     ┌──────────────────┐     ┌─────────────────┐
│   Telegram Bot  │     │  Liquidity Pool  │     │  Audit Logs     │
│  (Notifications)│     │  (Company Funds) │     │  (History)      │
└─────────────────┘     └──────────────────┘     └─────────────────┘
```

### Компоненты системы

1. **Frontend (React/Next.js)**
   - Компонент автоматического обмена
   - Компонент ордербука
   - История операций
   - Управление ордерами

2. **Backend API (Next.js Route Handlers)**
   - Обработка автоматических обменов
   - Управление ордерами
   - Сопоставление ордеров (matching engine)
   - Управление пулом ликвидности

3. **Database (PostgreSQL)**
   - Таблица ордеров
   - Таблица автоматических обменов
   - Таблица пула ликвидности
   - История операций

4. **Smart Contract Integration**
   - Передача токенов при обмене
   - Проверка балансов
   - События транзакций

---

## 📊 Структура базы данных

### Таблица: `trading_orders`

Хранение ордеров пользователей.

```sql
CREATE TABLE IF NOT EXISTS trading_orders (
  id VARCHAR(50) PRIMARY KEY,
  user_id UUID REFERENCES users(id) ON DELETE CASCADE,
  wallet_address VARCHAR(255) NOT NULL,
  order_type VARCHAR(10) NOT NULL CHECK (order_type IN ('BUY', 'SELL')),
  token_amount NUMERIC(78, 0) NOT NULL,
  eur_amount NUMERIC(18, 2) NOT NULL,
  price_per_token NUMERIC(18, 6) NOT NULL, -- EUR per token
  filled_token_amount NUMERIC(78, 0) DEFAULT 0 NOT NULL,
  filled_eur_amount NUMERIC(18, 2) DEFAULT 0 NOT NULL,
  status VARCHAR(20) DEFAULT 'pending' NOT NULL CHECK (status IN ('pending', 'partially_filled', 'filled', 'cancelled', 'rejected')),
  expires_at TIMESTAMP,
  created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
  updated_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
  cancelled_at TIMESTAMP,
  filled_at TIMESTAMP
);

CREATE INDEX IF NOT EXISTS idx_trading_orders_user ON trading_orders(user_id);
CREATE INDEX IF NOT EXISTS idx_trading_orders_status ON trading_orders(status);
CREATE INDEX IF NOT EXISTS idx_trading_orders_type ON trading_orders(order_type);
CREATE INDEX IF NOT EXISTS idx_trading_orders_price ON trading_orders(price_per_token);
CREATE INDEX IF NOT EXISTS idx_trading_orders_created ON trading_orders(created_at DESC);
```

### Таблица: `trading_auto_exchanges`

История автоматических обменов.

```sql
CREATE TABLE IF NOT EXISTS trading_auto_exchanges (
  id VARCHAR(50) PRIMARY KEY,
  user_id UUID REFERENCES users(id) ON DELETE CASCADE,
  wallet_address VARCHAR(255) NOT NULL,
  exchange_type VARCHAR(10) NOT NULL CHECK (exchange_type IN ('TOKEN_TO_EUR', 'EUR_TO_TOKEN')),
  token_amount NUMERIC(78, 0) NOT NULL,
  eur_amount NUMERIC(18, 2) NOT NULL,
  rate NUMERIC(18, 6) NOT NULL, -- Actual rate used
  commission_amount NUMERIC(18, 2) NOT NULL,
  commission_percent NUMERIC(5, 2) NOT NULL,
  tx_hash VARCHAR(66), -- Blockchain transaction hash
  status VARCHAR(20) DEFAULT 'pending' NOT NULL CHECK (status IN ('pending', 'processing', 'completed', 'failed', 'cancelled')),
  error_message TEXT,
  created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
  updated_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
  completed_at TIMESTAMP
);

CREATE INDEX IF NOT EXISTS idx_trading_auto_exchanges_user ON trading_auto_exchanges(user_id);
CREATE INDEX IF NOT EXISTS idx_trading_auto_exchanges_status ON trading_auto_exchanges(status);
CREATE INDEX IF NOT EXISTS idx_trading_auto_exchanges_tx_hash ON trading_auto_exchanges(tx_hash);
CREATE INDEX IF NOT EXISTS idx_trading_auto_exchanges_created ON trading_auto_exchanges(created_at DESC);
```

### Таблица: `trading_order_matches`

История сопоставления ордеров.

```sql
CREATE TABLE IF NOT EXISTS trading_order_matches (
  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  buy_order_id VARCHAR(50) REFERENCES trading_orders(id) ON DELETE CASCADE,
  sell_order_id VARCHAR(50) REFERENCES trading_orders(id) ON DELETE CASCADE,
  token_amount NUMERIC(78, 0) NOT NULL,
  eur_amount NUMERIC(18, 2) NOT NULL,
  match_price NUMERIC(18, 6) NOT NULL,
  created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);

CREATE INDEX IF NOT EXISTS idx_trading_order_matches_buy ON trading_order_matches(buy_order_id);
CREATE INDEX IF NOT EXISTS idx_trading_order_matches_sell ON trading_order_matches(sell_order_id);
CREATE INDEX IF NOT EXISTS idx_trading_order_matches_created ON trading_order_matches(created_at DESC);
```

### Таблица: `trading_liquidity_pool`

Управление пулом ликвидности компании.

```sql
CREATE TABLE IF NOT EXISTS trading_liquidity_pool (
  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  token_balance NUMERIC(78, 0) DEFAULT 0 NOT NULL,
  eur_balance NUMERIC(18, 2) DEFAULT 0 NOT NULL,
  bid_price NUMERIC(18, 6) NOT NULL, -- Price company buys tokens
  ask_price NUMERIC(18, 6) NOT NULL, -- Price company sells tokens
  spread_percent NUMERIC(5, 2) NOT NULL, -- Bid-ask spread
  min_order_amount NUMERIC(78, 0), -- Minimum token amount for auto exchange
  max_order_amount NUMERIC(78, 0), -- Maximum token amount for auto exchange
  is_active BOOLEAN DEFAULT TRUE NOT NULL,
  updated_by VARCHAR(255), -- Admin who updated
  updated_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
  created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);

-- Only one active pool configuration
CREATE UNIQUE INDEX IF NOT EXISTS idx_trading_liquidity_pool_active ON trading_liquidity_pool(is_active) WHERE is_active = TRUE;
```

### Таблица: `trading_pool_transactions`

История операций с пулом ликвидности.

```sql
CREATE TABLE IF NOT EXISTS trading_pool_transactions (
  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  pool_id UUID REFERENCES trading_liquidity_pool(id) ON DELETE CASCADE,
  transaction_type VARCHAR(20) NOT NULL CHECK (transaction_type IN ('DEPOSIT_TOKEN', 'WITHDRAW_TOKEN', 'DEPOSIT_EUR', 'WITHDRAW_EUR', 'AUTO_EXCHANGE', 'ORDER_FILL')),
  token_amount NUMERIC(78, 0),
  eur_amount NUMERIC(18, 2),
  token_balance_after NUMERIC(78, 0) NOT NULL,
  eur_balance_after NUMERIC(18, 2) NOT NULL,
  reference_id VARCHAR(50), -- ID of exchange or order
  metadata JSONB,
  created_by VARCHAR(255),
  created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);

CREATE INDEX IF NOT EXISTS idx_trading_pool_transactions_pool ON trading_pool_transactions(pool_id);
CREATE INDEX IF NOT EXISTS idx_trading_pool_transactions_type ON trading_pool_transactions(transaction_type);
CREATE INDEX IF NOT EXISTS idx_trading_pool_transactions_created ON trading_pool_transactions(created_at DESC);
```

### Триггеры

```sql
-- Update updated_at timestamp
CREATE TRIGGER update_trading_orders_updated_at
  BEFORE UPDATE ON trading_orders
  FOR EACH ROW
  EXECUTE FUNCTION update_updated_at_column();

CREATE TRIGGER update_trading_auto_exchanges_updated_at
  BEFORE UPDATE ON trading_auto_exchanges
  FOR EACH ROW
  EXECUTE FUNCTION update_updated_at_column();

CREATE TRIGGER update_trading_liquidity_pool_updated_at
  BEFORE UPDATE ON trading_liquidity_pool
  FOR EACH ROW
  EXECUTE FUNCTION update_updated_at_column();
```

---

## 🔌 API Endpoints

### Автоматический обмен

#### `POST /api/trading/auto-exchange`

Создание автоматического обмена.

**Request Body:**
```typescript
{
  exchangeType: 'TOKEN_TO_EUR' | 'EUR_TO_TOKEN';
  amount: string; // Token amount or EUR amount
  walletAddress: string;
  email: string;
}
```

**Response:**
```typescript
{
  success: boolean;
  exchangeId: string;
  tokenAmount: string;
  eurAmount: string;
  rate: string;
  commission: string;
  status: 'pending' | 'processing' | 'completed' | 'failed';
  txHash?: string;
}
```

#### `GET /api/trading/auto-exchange/:id`

Получение статуса автоматического обмена.

**Response:**
```typescript
{
  id: string;
  exchangeType: string;
  tokenAmount: string;
  eurAmount: string;
  rate: string;
  commission: string;
  status: string;
  txHash?: string;
  createdAt: string;
  updatedAt: string;
}
```

#### `GET /api/trading/auto-exchange`

Получение истории автоматических обменов пользователя.

**Query Parameters:**
- `status?: string` - Фильтр по статусу
- `type?: string` - Фильтр по типу обмена
- `limit?: number` - Лимит записей (default: 50)
- `offset?: number` - Смещение (default: 0)

### Ордербук

#### `POST /api/trading/orders`

Создание нового ордера.

**Request Body:**
```typescript
{
  orderType: 'BUY' | 'SELL';
  tokenAmount: string;
  pricePerToken: string; // EUR per token
  expiresAt?: string; // ISO timestamp, optional
}
```

**Response:**
```typescript
{
  success: boolean;
  orderId: string;
  status: 'pending' | 'rejected';
  message?: string;
}
```

#### `GET /api/trading/orders`

Получение списка ордеров.

**Query Parameters:**
- `type?: 'BUY' | 'SELL'` - Фильтр по типу
- `status?: string` - Фильтр по статусу
- `limit?: number` - Лимит записей (default: 50)
- `offset?: number` - Смещение (default: 0)

**Response:**
```typescript
{
  orders: Array<{
    id: string;
    orderType: string;
    tokenAmount: string;
    eurAmount: string;
    pricePerToken: string;
    filledTokenAmount: string;
    filledEurAmount: string;
    status: string;
    createdAt: string;
  }>;
  total: number;
}
```

#### `GET /api/trading/orders/book`

Получение ордербука (активные ордера для отображения).

**Query Parameters:**
- `side?: 'BUY' | 'SELL' | 'BOTH'` - Сторона ордербука (default: 'BOTH')
- `limit?: number` - Количество ордеров на сторону (default: 20)

**Response:**
```typescript
{
  buyOrders: Array<{
    id: string;
    pricePerToken: string;
    tokenAmount: string;
    filledTokenAmount: string;
    createdAt: string;
  }>;
  sellOrders: Array<{
    id: string;
    pricePerToken: string;
    tokenAmount: string;
    filledTokenAmount: string;
    createdAt: string;
  }>;
  bestBid: string; // Best buy price
  bestAsk: string; // Best sell price
  spread: string; // Spread percentage
}
```

#### `DELETE /api/trading/orders/:id`

Отмена ордера.

**Response:**
```typescript
{
  success: boolean;
  message: string;
}
```

#### `GET /api/trading/orders/:id`

Получение деталей ордера.

**Response:**
```typescript
{
  id: string;
  orderType: string;
  tokenAmount: string;
  eurAmount: string;
  pricePerToken: string;
  filledTokenAmount: string;
  filledEurAmount: string;
  status: string;
  matches: Array<{
    id: string;
    matchedOrderId: string;
    tokenAmount: string;
    eurAmount: string;
    matchPrice: string;
    createdAt: string;
  }>;
  createdAt: string;
  updatedAt: string;
}
```

### Управление пулом ликвидности (Admin)

#### `GET /api/trading/pool`

Получение текущего состояния пула.

**Response:**
```typescript
{
  tokenBalance: string;
  eurBalance: string;
  bidPrice: string;
  askPrice: string;
  spreadPercent: string;
  minOrderAmount: string;
  maxOrderAmount: string;
  isActive: boolean;
}
```

#### `POST /api/trading/pool/update` (Admin only)

Обновление параметров пула.

**Request Body:**
```typescript
{
  bidPrice?: string;
  askPrice?: string;
  spreadPercent?: string;
  minOrderAmount?: string;
  maxOrderAmount?: string;
  isActive?: boolean;
}
```

**Headers:**
- `Authorization: Bearer <admin_token>`

#### `POST /api/trading/pool/deposit` (Admin only)

Пополнение пула.

**Request Body:**
```typescript
{
  type: 'TOKEN' | 'EUR';
  amount: string;
}
```

#### `POST /api/trading/pool/withdraw` (Admin only)

Вывод из пула.

**Request Body:**
```typescript
{
  type: 'TOKEN' | 'EUR';
  amount: string;
}
```

### История операций

#### `GET /api/trading/history`

Получение истории всех операций пользователя.

**Query Parameters:**
- `type?: 'AUTO_EXCHANGE' | 'ORDER' | 'ALL'` - Тип операции
- `status?: string` - Фильтр по статусу
- `fromDate?: string` - Начальная дата (ISO)
- `toDate?: string` - Конечная дата (ISO)
- `limit?: number` - Лимит записей (default: 50)
- `offset?: number` - Смещение (default: 0)

**Response:**
```typescript
{
  history: Array<{
    id: string;
    type: 'AUTO_EXCHANGE' | 'ORDER';
    operation: string; // Description
    tokenAmount: string;
    eurAmount: string;
    status: string;
    createdAt: string;
  }>;
  total: number;
}
```

#### `GET /api/trading/history/export`

Экспорт истории в CSV.

**Query Parameters:** Те же, что и для `/api/trading/history`

**Response:** CSV file download

---

## 🎨 UI/UX Требования

### Компонент: Автоматический обмен

**Расположение:** Отдельная страница `/trading` или секция на главной странице

**Элементы:**
1. Переключатель типа обмена (TOKEN → EUR / EUR → TOKEN)
2. Поле ввода количества (токены или EUR)
3. Отображение расчетной суммы получателя
4. Отображение текущего курса
5. Отображение комиссии
6. Кнопка "Обменять"
7. Индикатор статуса операции
8. История последних обменов

**Валидация:**
- Минимальная/максимальная сумма обмена
- Проверка баланса токенов
- Проверка доступности ликвидности в пуле

### Компонент: Ордербук

**Расположение:** Та же страница `/trading`, вкладка "Ордербук"

**Элементы:**
1. Таблица активных ордеров на покупку (BUY)
2. Таблица активных ордеров на продажу (SELL)
3. Форма создания нового ордера
4. Список моих ордеров с возможностью отмены
5. График цен (опционально)

**Отображение ордербука:**
- Сортировка по цене (BUY - по убыванию, SELL - по возрастанию)
- Группировка ордеров с одинаковой ценой
- Отображение суммарного объема на каждом уровне цены
- Цветовое кодирование (зеленый для BUY, красный для SELL)

### Компонент: История операций

**Расположение:** Страница `/trading/history` или вкладка в профиле

**Элементы:**
1. Фильтры (тип операции, статус, дата)
2. Таблица операций с деталями
3. Пагинация
4. Кнопка экспорта в CSV
5. Детальный просмотр операции (модальное окно)

### Компонент: Управление пулом (Admin)

**Расположение:** `/admin/trading/pool`

**Элементы:**
1. Текущий баланс пула (токены и EUR)
2. Текущие цены (bid/ask)
3. Форма обновления параметров
4. История операций с пулом
5. График изменения баланса (опционально)

---

## 🔒 Безопасность

### Валидация операций

1. **Проверка баланса**
   - Перед автоматическим обменом проверять достаточность баланса токенов
   - Перед созданием ордера проверять достаточность баланса

2. **Лимиты**
   - Минимальная сумма обмена/ордера
   - Максимальная сумма обмена/ордера
   - Лимит на количество активных ордеров пользователя

3. **Rate Limiting**
   - Ограничение количества запросов на создание обменов/ордеров
   - Защита от спама и злоупотреблений

4. **Проверка подписи транзакций**
   - Все операции с токенами требуют подписи через MetaMask
   - Валидация транзакций на блокчейне

### Административные функции

1. **Авторизация админа**
   - Проверка токена авторизации для операций с пулом
   - Логирование всех административных действий

2. **Мониторинг**
   - Алерты при низком балансе пула
   - Алерты при подозрительной активности
   - Логирование всех операций

---

## ⚙️ Бизнес-логика

### Автоматический обмен

**Процесс:**
1. Пользователь указывает тип обмена и количество
2. Система проверяет баланс пользователя
3. Система проверяет доступность ликвидности в пуле
4. Система рассчитывает курс с учетом спреда
5. Система рассчитывает комиссию
6. Пользователь подтверждает операцию
7. Система создает транзакцию на блокчейне:
   - Для TOKEN → EUR: перевод токенов от пользователя в пул
   - Для EUR → TOKEN: перевод токенов из пула пользователю
8. После подтверждения транзакции обновляется баланс пула
9. Пользователь получает уведомление

**Расчет курса:**
- Для TOKEN → EUR: используется `ask_price` (цена продажи компании)
- Для EUR → TOKEN: используется `bid_price` (цена покупки компании)
- Комиссия вычитается из суммы получателя

### Ордербук и сопоставление

**Процесс создания ордера:**
1. Пользователь указывает тип ордера, количество и цену
2. Система проверяет баланс пользователя
3. Система проверяет лимиты (мин/макс сумма)
4. Система пытается сопоставить ордер с существующими ордерами
5. Если сопоставление возможно - частичное/полное исполнение
6. Если сопоставление невозможно - ордер добавляется в ордербук

**Сопоставление ордеров (Matching Engine):**
- BUY ордер сопоставляется с SELL ордерами, где цена SELL <= цена BUY
- SELL ордер сопоставляется с BUY ордерами, где цена BUY >= цена SELL
- Приоритет: цена (лучшая цена первая), затем время создания (FIFO)
- Частичное исполнение возможно

**Исполнение ордера:**
1. При сопоставлении создается запись в `trading_order_matches`
2. Обновляется статус обоих ордеров
3. Выполняются транзакции на блокчейне:
   - Токены переводятся от продавца покупателю
   - EUR переводятся от покупателя продавцу (через пул или напрямую)
4. Обновляются балансы пользователей
5. Отправляются уведомления обоим пользователям

**Отмена ордера:**
- Пользователь может отменить свой ордер в статусе `pending` или `partially_filled`
- Отмененный ордер не может быть исполнен
- Заблокированные средства возвращаются пользователю

### Управление пулом ликвидности

**Обновление параметров:**
- Администратор может изменить bid/ask цены
- Администратор может изменить спред
- Администратор может установить лимиты мин/макс суммы
- Администратор может активировать/деактивировать пул

**Пополнение/вывод:**
- Администратор может пополнить пул токенами или EUR
- Администратор может вывести средства из пула
- Все операции логируются в `trading_pool_transactions`

---

## 📱 Интеграции

### Telegram Bot

**Уведомления:**
- Уведомление о создании нового ордера (если цена близка к текущей)
- Уведомление об исполнении ордера
- Уведомление об отмене ордера
- Уведомление о завершении автоматического обмена
- Уведомления администратору о критических событиях (низкий баланс пула)

**Команды (для админа):**
- `/trading-pool` - Статус пула ликвидности
- `/trading-orders` - Список активных ордеров
- `/trading-stats` - Статистика торговли

### Smart Contract Events

**События для отслеживания:**
- `Transfer` - Перевод токенов
- Синхронизация балансов после транзакций
- Обновление статусов операций

---

## 🚀 Этапы реализации

### Этап 1: База данных и миграции (2-3 дня)

**Задачи:**
- [ ] Создать SQL миграции для всех таблиц
- [ ] Создать индексы и триггеры
- [ ] Создать функции для работы с балансами пула
- [ ] Написать тесты для миграций

**Результат:**
- Все таблицы созданы в базе данных
- Индексы оптимизированы для производительности
- Триггеры работают корректно

### Этап 2: Backend API - Автоматический обмен (3-4 дня)

**Задачи:**
- [ ] Реализовать `POST /api/trading/auto-exchange`
- [ ] Реализовать `GET /api/trading/auto-exchange/:id`
- [ ] Реализовать `GET /api/trading/auto-exchange`
- [ ] Интеграция с пулом ликвидности
- [ ] Интеграция со смарт-контрактом
- [ ] Обработка ошибок и валидация
- [ ] Написать тесты

**Результат:**
- Работающий API для автоматического обмена
- Интеграция с блокчейном
- Обработка всех edge cases

### Этап 3: Backend API - Ордербук (4-5 дней)

**Задачи:**
- [ ] Реализовать `POST /api/trading/orders`
- [ ] Реализовать `GET /api/trading/orders`
- [ ] Реализовать `GET /api/trading/orders/book`
- [ ] Реализовать `DELETE /api/trading/orders/:id`
- [ ] Реализовать `GET /api/trading/orders/:id`
- [ ] Реализовать matching engine
- [ ] Обработка частичного исполнения
- [ ] Написать тесты

**Результат:**
- Работающий ордербук
- Matching engine корректно сопоставляет ордера
- Поддержка частичного исполнения

### Этап 4: Backend API - Управление пулом (2-3 дня)

**Задачи:**
- [ ] Реализовать `GET /api/trading/pool`
- [ ] Реализовать `POST /api/trading/pool/update`
- [ ] Реализовать `POST /api/trading/pool/deposit`
- [ ] Реализовать `POST /api/trading/pool/withdraw`
- [ ] Административная авторизация
- [ ] Логирование операций
- [ ] Написать тесты

**Результат:**
- API для управления пулом ликвидности
- Безопасная административная авторизация
- Полное логирование операций

### Этап 5: Backend API - История операций (1-2 дня)

**Задачи:**
- [ ] Реализовать `GET /api/trading/history`
- [ ] Реализовать `GET /api/trading/history/export`
- [ ] Оптимизация запросов с пагинацией
- [ ] Написать тесты

**Результат:**
- API для получения истории операций
- Экспорт в CSV работает корректно

### Этап 6: Frontend - Компоненты автоматического обмена (3-4 дня)

**Задачи:**
- [ ] Создать компонент `AutoExchangeForm`
- [ ] Создать компонент `ExchangeCalculator`
- [ ] Создать компонент `ExchangeHistory`
- [ ] Интеграция с API
- [ ] Обработка состояний загрузки и ошибок
- [ ] Адаптивный дизайн
- [ ] Интернационализация

**Результат:**
- Работающий UI для автоматического обмена
- Красивый и удобный интерфейс
- Поддержка всех языков

### Этап 7: Frontend - Компоненты ордербука (4-5 дней)

**Задачи:**
- [ ] Создать компонент `OrderBook`
- [ ] Создать компонент `OrderForm`
- [ ] Создать компонент `MyOrders`
- [ ] Реализовать обновление в реальном времени (WebSocket или polling)
- [ ] Интеграция с API
- [ ] Адаптивный дизайн
- [ ] Интернационализация

**Результат:**
- Работающий UI для ордербука
- Обновление данных в реальном времени
- Удобное управление ордерами

### Этап 8: Frontend - История операций (2 дня)

**Задачи:**
- [ ] Создать компонент `TradingHistory`
- [ ] Реализовать фильтры
- [ ] Реализовать экспорт в CSV
- [ ] Интеграция с API
- [ ] Адаптивный дизайн
- [ ] Интернационализация

**Результат:**
- Работающий UI для истории операций
- Удобные фильтры и экспорт

### Этап 9: Frontend - Админ панель управления пулом (2-3 дня)

**Задачи:**
- [ ] Создать компонент `LiquidityPoolManagement`
- [ ] Создать компонент `PoolTransactions`
- [ ] Интеграция с API
- [ ] Визуализация баланса пула
- [ ] Адаптивный дизайн

**Результат:**
- Работающая админ панель
- Удобное управление пулом

### Этап 10: Интеграция с Telegram Bot (1-2 дня)

**Задачи:**
- [ ] Добавить команды для админа
- [ ] Реализовать уведомления пользователям
- [ ] Реализовать уведомления админу о критических событиях

**Результат:**
- Интеграция с Telegram Bot работает
- Все уведомления отправляются корректно

### Этап 11: Тестирование и оптимизация (3-4 дня)

**Задачи:**
- [ ] E2E тестирование основных сценариев
- [ ] Нагрузочное тестирование matching engine
- [ ] Оптимизация запросов к базе данных
- [ ] Исправление найденных багов
- [ ] Документация API

**Результат:**
- Система протестирована и оптимизирована
- Документация готова

### Этап 12: Деплой и мониторинг (1-2 дня)

**Задачи:**
- [ ] Деплой на тестовую среду
- [ ] Тестирование на тестовой среде
- [ ] Настройка мониторинга
- [ ] Деплой на продакшен

**Результат:**
- Система работает на продакшене
- Мониторинг настроен

---

## 📝 Дополнительные требования

### Производительность

- Matching engine должен обрабатывать ордера за время < 100ms
- API endpoints должны отвечать за время < 500ms
- Обновление ордербука в реальном времени (polling каждые 2-5 секунд или WebSocket)

### Масштабируемость

- Поддержка до 1000 активных ордеров одновременно
- Поддержка до 100 обменов в минуту
- Оптимизация запросов к базе данных

### Мониторинг

- Логирование всех операций
- Метрики производительности
- Алерты при критических событиях
- Дашборд для администратора

### Документация

- API документация (OpenAPI/Swagger)
- Руководство пользователя
- Руководство администратора
- Комментарии в коде

---

## 🔄 Совместимость с существующим функционалом

### Интеграция с текущим обменником

- Текущий обменник (`exchange_requests`) остается для ручной обработки менеджерами
- Новая система торговли работает параллельно
- Пользователь может выбрать между автоматическим обменом и заявкой на ручную обработку

### Использование существующих компонентов

- Использовать существующую систему авторизации
- Использовать существующую систему уведомлений
- Использовать существующие компоненты UI (Card, Button, Modal и т.д.)

---

## 📊 Метрики успеха

### Технические метрики

- Время ответа API < 500ms (95 перцентиль)
- Успешность выполнения операций > 99%
- Время сопоставления ордеров < 100ms

### Бизнес-метрики

- Количество автоматических обменов в день
- Количество созданных ордеров в день
- Процент исполненных ордеров
- Объем торговли (токены и EUR)

---

## 🎯 Приоритеты реализации

### Must Have (MVP)

1. Автоматический обмен TOKEN → EUR
2. Базовый ордербук (создание и отмена ордеров)
3. Управление пулом ликвидности (базовое)
4. История операций

### Should Have

1. Автоматический обмен EUR → TOKEN
2. Matching engine для ордеров
3. Частичное исполнение ордеров
4. Экспорт истории в CSV

### Nice to Have

1. График цен
2. Расширенная аналитика
3. Уведомления в браузере (Push Notifications)
4. Мобильное приложение

---

## 📚 Ссылки на документацию

- [Архитектура проекта](./architecture.md)
- [Правила архитектуры](./arch-rules.md)
- [Требования проекта](./requirements.md)
- [План реализации обменника](./exchange-implementation-plan.md)
- [Внутренний баланс](./internal-balance-implementation-plan.md)

---

**Версия документа:** 1.0  
**Дата создания:** 2025-01-XX  
**Статус:** Готов к реализации



